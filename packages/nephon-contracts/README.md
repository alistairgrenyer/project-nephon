# nephon-contracts

Public library package containing versioned data transfer objects (DTOs), public domain enums, context structures, and cryptographic serialization utilities for the Nephon Constitutional Control Plane.

This package has **zero internal dependencies** on `nephon-core` or private graph engine logic.
